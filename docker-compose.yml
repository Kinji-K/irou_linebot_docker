
version: '3'
services:
    python3_line:
     build:
       context: .
       args:
         UID: $UID
         GID: $GID
     container_name: 'python3_line'
     working_dir: '/opt/working'
     tty: true
     volumes:
        - ./working:/opt/working